services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: epiai
      POSTGRES_PASSWORD: epiai_dev
      POSTGRES_DB: epiai
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
